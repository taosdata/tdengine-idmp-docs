# API Reference

This chapter documents the primary methods of each SDK module. For the complete interface specification, refer to the OpenAPI spec file (`idmp-v{SDK_VERSION}.json`) included in the SDK package, or browse the live [Swagger UI]({IDMP_HOST}/swagger-ui.html).

import DocCardList from '@theme/DocCardList';

<DocCardList />
