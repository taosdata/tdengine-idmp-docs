# API 参考

本章对 SDK 中各 API 模块的主要方法进行说明。完整的接口规范请参考 SDK 压缩包中的 OpenAPI 规范文件（`idmp-v{SDK_VERSION}.json`），也可在线查看 Swagger UI（访问 IDMP 服务的 `/swagger-ui.html` 路径）。

import DocCardList from '@theme/DocCardList';

<DocCardList />
